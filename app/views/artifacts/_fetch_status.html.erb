<span class="artifact-item__badge <%= fetching ? 'artifact-item__badge--fetching' : 'artifact-item__badge--pending' %>" id="artifact_<%= artifact.id %>_status">
  <%= fetching ? '⟳ Fetching' : 'Pending' %>
</span>
